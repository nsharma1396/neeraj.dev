# Cloudflare Pages (static): deploy with `npx wrangler pages deploy dist`
# For static output, use: yarn build && npx wrangler pages deploy dist
name = "neeraj-dev"
compatibility_date = "2024-01-01"
pages_build_output_dir = "dist"
